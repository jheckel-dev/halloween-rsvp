{{!-- LOGIN --}}
{{!-- button --}}
{{!-- click > prompt for user input --}}
{{!-- login overlay --}}
{{!-- fields for credentials --}}
{{!-- submit // if correct, then redirect tooo the attendee (listing) page --}}
{{!-- if incorrect then prompt // error --}}
{{!-- cancel login, hide overlay --}}

{{#if condition}}
<h1>show me the partial</h1>
{{/if}}


{{#if message}}
<div class="alert alert-danger alert-dismissible fade show" role="alert">
  {{{message}}}
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">×</span>
  </button>
</div>
{{/if}}


<div class="row mt-5">
  <div class="col-md-5 m-auto">
    <div class="card card-body">
      <h1 class="text-center mb-3">Login</h1>
      {{>messages}}
      <form action="/users/login" method="POST">
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" class="form-control" name="email" placeholder="Enter Email" />
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" class="form-control" name="password" placeholder="Enter Password" />
        </div>
        <button type="submit" class="btn">Login</button>
        <a href="//" class="btn">Facebook Login</a>
      </form>
      <p class="lead">No Account?<a href="/users/register"> Register</a>
      </p>
    </div>
  </div>
</div>

{{!-- https://medium.com/minahuang/how-to-represent-login-ui-message-in-passport-js-localstrategy-with-express-handlebars-6bab60c0f62e --}}